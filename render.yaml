services:
  - type: web
    name: sustainable-energy-dashboard
    runtime: docker
    plan: starter
    autoDeploy: true
    disk:
      name: sustainable-energy-db
      mountPath: /app/db
      sizeGB: 1
